import React from 'react';
import Button from 'components/button';

export default React.createClass( {
	displayName: 'Settings',
	render: function() {
		return (
			<Button primary>It works!</Button>
		);
	}
} );
